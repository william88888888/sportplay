#mysql
spring:
  datasource:
    #mysql配置
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sportplay?serverTimezone=GMT%2B8
    username: root
    password: 888

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.demo.model

server:
  port: 9000